# 系統總體架構白皮書：R&S Echoes v8.8 (CEO 輔助生成與備援版)
**版本**：v8.8-Human-Assisted-Genesis  
**最後更新**：2026-04-03  
**目標環境**：Windows 11 (present~4/18/2026) / Mac mini M4 (4/18/2026 後部署)  
**核心戰略升級**：導入「自動供彈系統」與「CEO 手動空投」之混合架構。最大化利用 Suno 官方免費配額以保護現金流，將 TTAPI 降級為備援引擎，並確立最高修憲權限與靶場清空鐵律。

> **v9.5 戰略修正 (The Cost-Optimized Weekly Genesis)**：  
> 經財務審計與成本最佳化決策，產線正式由「每日高頻打靶」轉向「每週精準供彈」。系統**每週僅需生成 10 組「子彈」**供 CEO 於 Suno 網頁端打靶。滿意的成品空投回產線後，將全面啟動 **Protocol L (音樂資產保鮮庫)**。
> 透過 FFmpeg 的零成本變速/變調引擎，這 10 首母帶將被進行嚴格的「三振防線」重製 (derivation_count < 3)，裂變出足以支撐全週、多頻道的發行量，將 API 成本與手動作業時間壓縮至最低極限。

### 🛡️ 產線防禦與基礎設施鐵律 (The Ironclad Rules)
* **雙棲實體路徑隔離**：透過 `.env` 檔案中的 `WORKSPACE_ROOT` 變數與 `env_manager.py` 進行跨平台路徑解析。所有大容量 I/O 必須寫入此隔離區。Windows 開發期設定為本地目錄（如 `F:\AI_Workspace\AI_DRAMA_FACTORY`），Mac 部署後強制綁定 `/Volumes/AI_Workspace/` 保護 256GB SSD。
* **標準化退出碼與日誌瘦身 (Log Sanitization)**：所有 Python 腳本在捕捉到致命錯誤時，必須強制呼叫 `sys.exit(1)`。寫入 `project_learning.md` 的錯誤日誌必須經過正則清洗，嚴禁寫入動輒數十行的 Stack Trace，僅保留「Exception Name」與「最後一行關鍵錯誤訊息」，防止檔案無限膨脹導致復盤 Token 暴增。
* **指令注入防禦 (Command Injection Prevention)**：在 `auto_editor.py` 或任何呼叫 FFmpeg 等底層命令的腳本中，嚴禁使用 `shell=True`。所有 Subprocess 必須以陣列形式傳遞參數 `["ffmpeg", "-i", ...]`，並對 LLM 生成的檔名/路徑強制執行安全字元過濾，防止惡意 Prompt 注入導致 `rm -rf /` 災難。
* **原生外部機密隔離 (External Secrets)**：徹底淘汰實體 `.env` 檔案。全面導入 OpenClaw 的「外部機密管理」，透過終端機執行 `openclaw secrets configure` 寫入 API 金鑰。腳本運行時透過 `target-path` 驗證動態調用，實現「零明文落地」的軍規級防護。
* **API 級別結構化輸出 (Structured Outputs)**：捨棄易脆斷的正則表達式清洗。所有呼叫大模型的腳本，必須在 API 請求層面強制啟用 `response_format={ "type": "json_object" }` 或給定嚴格 JSON Schema。從底層強制約束 100% 合法 JSON，將防呆成本降至 $0。
* **原子寫入防護 (Atomic Writes)**：針對憑證檔 (`state.json`) 與狀態池，嚴禁直接 `open(file, 'w')`。必須強制寫入 `.tmp` 暫存檔，寫入成功後再呼叫作業系統層級的 `os.replace()` 覆蓋原檔。確保在斷電或磁碟瞬斷時，JSON 檔案 100% 不會發生 0KB 靜默損毀。
* **分卷與首尾影格繼承 (Chunking & Frame Inheritance)**：將 5~10 分鐘的劇本切割為多個 60 秒的卷宗 (Volumes)。在呼叫 Veo API 時，下一卷的第一個分鏡，必須強制讀取上一卷最後一個分鏡的「最後一幀 (Last Frame)」作為 Image Prompt 參考起點，達成 100% 無縫連貫。
* **API 限流與退避防護 (Rate Limit & Exponential Backoff)**：嚴禁在呼叫 Google Cloud API 遇到 `429 Too Many Requests` 時直接崩潰。所有 API 請求必須包裝在指數退避 (Exponential Backoff) 迴圈中，確保產線能在配額受限時優雅休眠與重試。
* **內容合規護欄 (Safety Guardrails)**：Google Veo 具備嚴格的安全審查。LLM 在生成分鏡 Prompt 時，必須注入系統級過濾指令，嚴禁出現暴力、自殘或血腥字眼，一律以「荒謬、誇張的定格動畫肢體」來隱喻職場壓抑，確保 API 通過率 100%。
* **媒體生命週期管理 (Media Lifecycle Management)**：嚴格執行「大檔案視為消耗品，小檔案視為永久資產」。1小時的 MP4 影片在上傳/推流後 48 小時內，必須強制從本機 SSD 刪除 (TTL=48h)。產線中繼暫存檔 (`temp_*.wav`) 必須在腳本結束時立即抹除 (Garbage Collection)。高商業價值的 WAV 母帶保留 7 天後，必須自動打包轉移至 S3/B2 冷儲存，釋放 Mac mini 空間。
* **Telegram-First 行動控制協議 (Mobile-Centric Control)**：產線所有關鍵決策點 (Checkpoint) 必須透過 Telegram Inline Keyboard (內嵌按鈕) 實現「一鍵化」操作。系統嚴禁要求 CEO 回到電腦端輸入指令。所有的「採用/放棄」或「確認發行」回饋，必須在手機端 1 秒內完成。
* **試聽與配額機制 (Audition & Quota)**：Phase 1 導入「Demo 審核機制」。系統自動截取原始音軌前 60 秒作為試聽檔。每日產線必須達成「10 首 CEO 採用」之目標。若未滿 10 首，`pipeline_runner.py` 需持續運作直至配額達標，確保每日庫存水位穩定。
* **最高修憲權限鎖定 (Constitution Immutability)**：嚴禁工程團隊、Copilot 或任何 AI 探員擅自修改產線憲法！任何關於 `.clinerules`, `copilot-instructions.md`, `project.yml` 或 `架構說明書` 的核心文件更新，**僅限 CEO 或 CTO 手動執行**。工程團隊若試圖透過腳本或 Agent 自動修改上述文件，將被視為嚴重越權行為。
* **防流程偏移與備援隔離鐵律 (Pipeline Drift & Fallback Discipline)**：確立 `pipeline_runner.py` 為產線單一入口。產線的優先觸發源必須是 `ceo_approved_beats/` 內的 CEO 手動空投檔。TTAPI 雲端網關已降級為「備援引擎」，僅在庫存數量不足時才允許觸發 `suno_api_engine.py` 進行補充。每日任務達成後，必須強制呼叫 `os.remove()` 清空 `ceo_approved_beats/`，嚴禁隔日重複使用舊檔。
* **資產永續與雙軸標籤鐵律 (Asset Vault & Dual-Axis Tagging)**：為實現跨頻道矩陣營運與極致成本控制，所有通過 CEO 審核之音軌，嚴禁輕易刪除。必須統一送入 `rs_music_vault.db`，並強制賦予「情緒氛圍 (Mood)」與「流派節奏 (Genre/BPM)」雙維度標籤。
* **三振再製防線 (The Rule of 3 Derivations)**：為避免平台演算法降權與聽覺疲勞，同一首入庫母帶的「二次利用 (變速、變調、重新混音或 Audio Prompting)」，其生命週期內絕對禁止超過 3 次。資料庫必須嚴格追蹤 `derivation_count` 欄位。


## ▌ 1. 基礎設施與開源技術棧 (環境部署清單)

開源系統總覽：

🛠️ 核心開源技術棧大解密
1. 大小腦協同與智能體框架 (Agent & Logic Routing)
* **OpenClaw：** 產線的 Meta-Agent 總管。
* **智譜 AI (GLM-4-Flash)：** 產線的大腦。除負責生成高質感 DistroKid 發行企劃書外，新增「自動供彈」任務：讀取 `.openclaw\GL_4M_Suno_prompt.md` 作為 System Prompt，每日自動生成 20 組精準的 Suno 提示詞供 CEO 打靶。

2. 企業級 API 算力引擎 (Media Generation API)
* **Midjourney API / Kling (I2V)：** 產線的「視覺基底」。生成 4K 峽谷/雪山/書房靜態圖，並轉化為 5 秒的 Cinemagraph (微動態：呼吸、流水、微風) 母帶。
* **Suno API (TTAPI 雲端聚合網關 - 備援引擎)：** 產線的「備用 Lofi 節拍器」。日常生產主要依賴 CEO 於 Suno 網頁端之免費額度生成；僅當每日庫存不達標時，才對接 TTAPI 企業級雲端代理 (`https://api.ttapi.io`) 進行補足。

3. 極限影音與母帶處理引擎 (Media & Audio Processing)
* **FFmpeg / SoX：** 產線的「自動母帶工程師與剪輯師」。
  - **音訊端**：全面廢除 SoX 混響與 SFX 自然白噪音疊加，實施「純淨母帶處理 (Naked Mastering)」。僅保留 FFmpeg 執行嚴格的 LUFS 響度標準化 (YouTube -14, Spotify -16) 與格式轉換。
  - **視訊端**：利用 Ping-Pong 或 Crossfade 演算法，將 5 秒的微動態影片無損延伸至 1 小時，並與音軌完美縫合。
* **Liquidsoap (預備導入)：** 專為網路電台設計的推流腳本語言。用於 Mac mini M4 上，實現本地資料夾熱更新與 24/7 無人值守的 YouTube 串流直播。


【工程團隊注意】：請在 Mac M4 上執行以下指令，建立標準化開發與執行環境。我們不購買外部技能，全數使用穩定開源庫。

1. 系統級依賴 (透過 Homebrew)
請在終端機執行：
brew install ffmpeg

### 2. 雙棲環境依賴：Python 沙盒與 Node.js 中繼站
**CTO 鐵律：產線由 Python 驅動，Suno 音樂生成全面依賴 TTAPI 雲端代理。嚴禁在本地啟動任何 Node.js/Docker 爬蟲中繼站。**

**【A. TTAPI 雲端金鑰配置 (Cloud API Setup)】**
全面廢除本地 Docker 依賴。必須在專案根目錄的 .env 檔案中，配置有效的 TTAPI 金鑰與路由：
- **Windows Pre-production (目前~4/17/2026), Mac M4 (4/18/2026 後部署)**：
  1. 確認 .env 包含 TTAPI_KEY="您的_Master_Key"
  2. 確認 .env 包含 TT_BASE_URL="https://api.ttapi.io"
  

**【B. Python 主產線環境配置 (Virtual Environment)】**
請在專案根目錄 (`AI_DRAMA_FACTORY`) 下，開啟終端機並依序執行：

- **Windows 開發機**：
  1. `python -m venv venv`
  2. `.\venv\Scripts\activate`
  3. `pip install -r requirements.txt`
- **Mac M4 部署機**：
  1. `python3 -m venv venv`
  2. `source venv/bin/activate`
  3. `pip install -r requirements.txt`

請將以下內容存為 `requirements.txt`：
google-genai
librosa==0.10.1
yt-dlp==2024.03.10
Pillow==10.2.0
opencv-python==4.9.0
edge-tts==6.1.9
pyTelegramBotAPI==4.15.4
requests==2.31.0
# --- 新增 v8.7 RPA 與冷儲存核心依賴 ---
playwright==1.42.0
playwright-stealth==1.0.6
boto3==1.34.69

套件用途對照表（請一併更新）：

* requests: 用於讓 suno_api_engine.py 呼叫本機的 localhost:3000 中繼站。
* librosa: 音樂重拍 (Beats) 分析，用於踩點剪輯。
* yt-dlp: 礦車素材下載器。
* Pillow: 十二宮格影像拼接。
* opencv-python: 透過 SSIM 演算法進行影格去重 ($0 成本過濾)。
* edge-tts: 網路全斷時的終極地端高擬真配音兜底。
* pyTelegramBotAPI: 行動端一鍵授權與 1 分鐘 Demo 試聽遙控器。
* playwright & playwright-stealth： [🆕 新增] 驅動無頭瀏覽器，執行 DistroKid RPA 幽靈上傳，並規避 Cloudflare 反爬蟲偵測。在工程團隊執行完 pip install -r requirements.txt 之後，必須在終端機多執行一行指令來下載無頭瀏覽器（Chromium）的核心引擎，否則 RPA 依然無法啟動：`playwright install chromium`
* boto3： [🆕 新增] 呼叫 AWS S3 / Backblaze B2 協定，執行高商業價值 WAV 母帶的雲端冷儲存備份。


1. OpenClaw 底層權限與大腦同步 (project.yml & .clinerules)
* 沙盒權限解放：在 project.yml 的 role_exceptions 中，必須移除 $$VD (視覺導演) 的 playwright 權限，並重新開放 HTTP 請求權限與 Google Vertex AI 的網域白名單，允許其直連外部 API 算圖。
* 工具名單更新：將舊版依賴的 webhook_poller 等廢棄技能從 required_skills 清單中剔除，確保 Meta-Agent 不會因為找不到舊工具而陷入死循環。
* **運行時安全隔離 (OpenShell SSH Sandbox)**：全面擁抱 OpenClaw v2026.3.22 最新架構。將 RPA 引擎與 FFmpeg 等高風險/高負載腳本，全數投入輕量化 SSH 沙盒中執行。這能實現超越「路徑鎖死」的實體級物理隔離，杜絕任何 Agent 越權存取 Mac 主系統。
* **網關看門狗 (Gateway Defibrillator)**：無需安裝外部插件。利用 macOS 原生 `launchd`，為 OpenClaw 主程序與 LiteLLM 聚合網關編寫帶有 `KeepAlive=true` 的守護配置。一旦核心程序因網路波動崩潰，作業系統將在 3 秒內自動將其「電擊救活」，確保產線大腦全年無休。

1. 記憶體與檢索擴充預備 (Future Proofing & Zero-Cost Retrieval)
* **本地檢索神器 (QMD)**：針對未來膨脹的「風格基因庫」與「報錯日誌」，系統深度整合開源本地檢索工具 QMD (The Token Killer)。在 CTO 週末進行產線除錯與復盤時，直接開啟本地 OpenClaw Session 調用 QMD，透過本地 BM25 + 向量運算，精準從 `project_learning.md` 與 `dom_error_snapshot.txt` 抓取 Top-3 關鍵段落給模型分析，達成 $0 API 成本的高效復盤。


## ▌ 5. 專案完整檔案結構 (Windows 與 Mac 通用)

AI_DRAMA_FACTORY/                  # 專案總根目錄
│
├── .env                           # ⚙️ 全域環境變數
├── .clinerules                    # 🧠 OpenClaw 最高行為準則
├── project.yml                    # 🧠 R&S Echoes 產線大腦配置
│
├── .openclaw/                     # 🤖 Meta-Agent 配置與提示詞庫
│   └── GL_4M_Suno_prompt.md       # 📜 [🆕 新增] Suno 提示詞鐵律配置檔 (供 GLM-4 讀取)
│
├── scripts/                       # 🛠️ Python 自研腳本
│   ├── common/                    
│   └── gear1_prod/                # 🏭 齒輪一：R&S Echoes 數位唱片產線
│       ├── pipeline_runner.py             # 總指揮官 (庫存盤點與觸發器)
│       ├── generate_ceo_prompts.py        # [🆕 新增] 自動供彈系統 (生成 20 組 Prompt 並執行源頭防呆)
│       ├── suno_api_engine.py             # [⏬ 降級] TTAPI 備援引擎
│       ├── audio_mastering_engine.py      # 自動母帶處理 (LUFS 標準化)
│       ├── lofi_assembler.py              # 1 小時無縫縫合機 (Ping-Pong / Crossfade)
│       ├── music_metadata_engine.py       # DistroKid 發行企劃書產生器 (GLM 驅動)
│       └── radio_streamer.liq             # 預備: Liquidsoap 24/7 直播推流配置
│
└── assets/                        # 📁 動態資料庫
    ├── .ceo_prompts/              # 📜 [🆕 新增] 存放每日生成的 daily_prompts_{date}.txt 供彈庫
    ├── audio/
    │   ├── ceo_approved_beats/    # 👑 CEO 手動空投區 (新鮮出爐：每日處理完畢強制移出)
    │   ├── reusable_vault/        # ♻️ [🆕 新增] 素材保鮮庫 (供二次/三次混製使用)
    │   └── mastered_tracks/       # 經過母帶處理的發行級音軌
    ├── sfx/                       # 存放自然白噪音 (rain.wav, river.wav, vinyl_crackle.wav)
    ├── video_clips/               # 存放 5 秒 Lofi Girl 微動態母帶 (I2V)
    └── final_exports/             # 存放 1 小時 MP4 成片與 metadata.json 企劃書


## ▌A. 交付物定義（驗收標準）
每次成功跑完一個 Lofi 產線，輸出以下檔案到 `assets/final_exports/`：
1. `R_S_Echoes_VolX_1Hour.mp4`（供 YouTube 上傳或直播使用的 1 小時 4K 影片）
2. `Track_X_Mastered.wav`（符合 -16 LUFS 標準，供 CEO 手動上傳 DistroKid / Spotify 的無損音檔）
3. `metadata_distrokid.json`（包含英文曲名、曲風標籤、SEO 描述的全球發行企劃書）




## ▌C. R&S Echoes 產線工作流 (Lofi Assembly & Mastering Strategy)

### Phase 1 — 自動供彈與 CEO 空投 (Human-Assisted Genesis & Fallback)
**戰術**：
- **第一步：自動供彈系統 (Prompt Arsenal)**：產線啟動 `generate_ceo_prompts.py`。腳本讀取 `.openclaw\GL_4M_Suno_prompt.md` 作為 System Prompt，呼叫 GLM-4 引擎生成 20 組氛圍提示詞。程式將自動執行「源頭防呆」(驗證 Tags 包含純淨母帶字眼，且 Prompt 包含至少 6 個段落)，通過驗證者方可寫入 `assets/.ceo_prompts/` 供 CEO 使用。
- **第二步：CEO 手動空投 (Manual Generation & QA)**：CEO 每日於 Suno 網頁端貼上 Prompt 進行生成，挑選完美達標（長度 2:30 以上、無雜音）的成品，手動下載至 `assets/audio/ceo_approved_beats/`。
- **第三步：總指揮官盤點與素材回收 (Inventory & Vault Fallback)**：`pipeline_runner.py` 啟動時優先盤點 `ceo_approved_beats/`。若庫存不足，產線將優先從本地 `rs_music_vault.db` 抽調使用次數小於 3 次的歷史素材，透過程式碼進行變調/變速等 0 成本重組。若本地素材枯竭，才授權觸發 `suno_api_engine.py` 透過 TTAPI 補齊剩餘配額。
- **第四步：每日清空靶場 (Daily GC)**：當日所有核准之 MP3 順利進入母帶處理 (Phase 2) 後，系統強制呼叫 `os.remove()` 清空 `ceo_approved_beats/` 目錄，確保隔日產線歸零，杜絕舊檔重複處理。

### Phase 2 — 純淨母帶處理 (Naked Auto-Mastering)
**腳本**：`scripts/gear1_prod/audio_mastering_engine.py`
**戰術**：
- **廢除混音疊加 (Zero Foley/SFX)**：為確保 Lofi 音樂最純粹的放鬆聽感，全面停用大自然白噪音與空間混響 (Reverb) 的疊加邏輯。
- **工業級發行規格 (Loudness Normalization)**：僅保留 FFmpeg `-af loudnorm` 濾鏡進行精準響度標準化，強制轉換為 44.1kHz / 24-bit 無損 WAV 格式。
- **雙軌道輸出**：自動輸出 YouTube 專用版 (-14 LUFS) 與 Spotify 商業發行專用版 (-16 LUFS, 真實峰值 -2.0 dBTP)。

### Phase 3 — 視覺錨點與微動態 I2V (The Cinemagraph)
**戰術**：
- 利用 Kling 或 智譜 CogVideoX 生成 5 秒的極靜謐微動態影片 (Cinemagraph)。
- **核心鐵律**：絕對鎖定鏡頭 (Static Camera)。只允許水波流動、光影閃爍、人物呼吸。此影片作為產線的「視覺母帶」，無需頻繁重算，可無限次重複使用以攤平 API 成本。

### Phase 4 — 無縫縫合機 (1-Hour Loop Assembler)
**腳本**：`scripts/gear1_prod/lofi_assembler.py`
**戰術**：
- **畫面無限延伸**：讀取 5 秒的微動態母帶，使用 FFmpeg 濾鏡 (`reverse` 與 `concat`) 執行 Ping-Pong 倒轉法，或 Crossfade 交叉淡化法，將畫面無縫延伸至 1 小時。
- **音軌淡入淡出**：將 Phase 2 處理好的多首 Lofi 節拍，加上 5-10 秒的交叉淡入淡出，與 1 小時的畫面合併，封裝輸出 `R_S_Echoes_VolX_1Hour.mp4`。

### Phase 5 — 數位唱片企劃引擎與授權閘道 (Metadata & Authorization)
**腳本**：`scripts/gear1_prod/music_metadata_engine.py`
**戰術**：
- 呼叫智譜 GLM-4，依據本期音樂的氛圍（如：Midnight Rain, Coffeehouse），生成符合全球音樂發行商標準的 JSON 企劃書。
- 必須包含：高質感英文曲名 (Track Title)、精準曲風分類 (Sub-genre)、以及 YouTube 專用的 SEO 描述。
- **Telegram 授權閘道 (Human-in-the-Loop)**：企劃書生成後，透過 Telegram 發送給 CEO 預覽，並附帶 `[🚀 授權發行至 DistroKid]` 內嵌按鈕。產線在此暫停，等待 CEO 於手機端一鍵確認。

### Phase 6 — 幽靈代操上傳、直播與冷儲存 (RPA Ghost Uploader & Archiving)
**腳本**：`scripts/gear1_prod/distrokid_rpa_uploader.py` / `radio_streamer.liq` / `cloud_archiver.py`
**戰術**：
- **DistroKid RPA 上傳 (Ghost Uploader)**：接收到 Telegram 授權指令後，Mac mini 喚醒 Playwright 無頭瀏覽器。載入預先登入的持久化 Cookie (繞過 2FA)，模擬人類打字與點擊延遲，自動填寫表單、上傳 WAV 與封面、勾選版權聲明並送出。若遇防護阻擋則截圖回傳 Telegram 報警。
- **直播中樞**：發行確認後，觸發 `Liquidsoap` 服務監聽本地發行資料夾，實現無人值守的 YouTube 24/7 背景推流。
- **資產打包與空間防禦**：執行 `cloud_archiver.py`，將超過 7 天的高商業價值 WAV 母帶與 JSON 企劃書，透過 API 打包上傳至 **Backblaze B2 (或 AWS S3)** 極低成本冷儲存空間；並強制抹除本機超過 48 小時的 MP4 大檔，確保 Mac mini SSD 永不爆滿。

## ▌D. v8.7 待辦工程與落地指引 (R&S Echoes 啟動清單)
> 目標：全面廢棄舊有 V2V 算圖腳本，快速建立 1 小時 Lofi 影片縫合與數位唱片自動母帶產線。

**Phase 1: 視覺與無縫縫合 (優先執行)**
- 建立 `assets/video_clips/`，將 CEO 算出的 5 秒微動態 `RS_lofi_gril_02.mp4` 放入。
- 開發 `lofi_assembler.py`，導入 FFmpeg Ping-Pong (倒轉) 演算法，測試將 5 秒影片無縫拉長至 1 小時。

**Phase 2: 聽覺與母帶工程 (Audio Mastering)**
- 建立 `assets/audio/ceo_approved_beats/` 作為 CEO 專屬空投區。
- 修改 `audio_mastering_engine.py`，廢除 SoX 混響與 SFX 疊加，純粹使用 FFmpeg 確保輸出達到 Spotify 嚴格的 -16 LUFS / -2.0 dBTP 標準與 44.1kHz/24-bit 無損格式。

**Phase 3: 發行企劃引擎**
- 開發 `music_metadata_engine.py`，串接智譜 GLM-4，自動輸出 `metadata_distrokid.json`。

**Phase 4: 硬體部署 (Target: 4/18 Mac mini M4 到貨後)**
- 在 M4 環境下安裝 `Liquidsoap`，測試讀取 `assets/final_exports/` 資料夾並向 YouTube 進行 24/7 背景推流。


### E1. 數位資產與財務防護 (Ghost Uploader RPA 鐵律)
- **嚴禁使用傳統 API 暴力上傳**：為規避 DistroKid 的 Cloudflare 反爬蟲機制並保護版稅帳號，上傳動作嚴禁使用傳統 `requests` 傳送表單。
- **Telegram 一鍵遙控與原生瀏覽器擬真**：必須使用 Python + Playwright 搭配 `playwright-stealth` 擴充。腳本必須載入已通過 2FA 的 User Data Dir (持久化 Cookie)，並在點擊與填表間強制加入隨機延遲 (0.5s~1.5s)。發行決策權 100% 透過 Telegram 按鈕保留給 CEO，達到「手機一鍵遠端遙控 Mac mini 實體機代操」的最高安全級別。

### E2. 視覺母帶保護 (Static Camera Rule)
- 在呼叫任何 AI 生成 5 秒微動態影片 (I2V) 時，運鏡參數必須強制設定為「靜止 (Static)」。嚴禁任何鏡頭推進或平移，否則將導致 FFmpeg 無縫循環 (Ping-Pong) 產生嚴重閃爍破綻。

### E3. 基礎穩定性防線 (繼承 v8.5)
- **Atomic Writes**：所有的 JSON 企劃書 (`metadata_distrokid.json`) 必須使用原子寫入 (`.tmp` → `os.replace()`)。
- **指令注入防禦**：FFmpeg 與 SoX 的調用嚴禁使用 `shell=True`，必須以陣列形式傳遞參數。

* **E4. 零容忍靜默失敗與誠實回報 (No Silent Failure & Absolute Integrity)**：
  - **嚴禁靜默失敗**：程式碼中嚴禁使用空 `try-except` 或僅印出警告卻繼續執行的邏輯。若核心 API (如 Suno, DistroKid) 失敗，必須強制呼叫 `sys.exit(1)` 並寫入 `fatal_log`，寧可產線停擺也絕不允許產生「空資產」或「假成功」。
  - **日誌真實性**：系統日誌必須反映真實 API 回傳狀態。若發生 500 錯誤，嚴禁腳本跳過該步驟去抓取舊檔案來充數。發現此行為視為架構性違規，必須立即重置環境。

> 本白皮書即為 R&S Echoes 廠牌的最高執行準則。
> 專注於高音質母帶、極致的視覺微動態、以及零人工的 1 小時無限循環剪輯。我們用最低的算力成本，打造最具商業價值的數位陪伴資產。
