# 系統總體架構白皮書：提線社畜 v8.0 (Google Veo API 核動力版)
**版本**：v8.0-Epic-LongForm  
**最後更新**：2026-03-28  
**目標環境**：Mac mini M4 (Apple Silicon) + 外接 APFS 硬碟 `/Volumes/AI_Workspace/`  
**核心戰略升級**：Google Vertex AI (Veo)、首尾影格繼承 (First-Frame Inheritance)、分卷戰術 (Chunking)、無損拼接 (FFmpeg Concat)、Structured Outputs、Atomic Writes  

> **v8.0 戰略轉向宣告**：  
> 本專案正式捨棄「即夢網頁端 RPA」的極限白嫖路線。為達成 **5~10 分鐘 Surreal AI 級別的無縫連貫微電影**，產線全面切換至 **Google Veo API**。透過「1 分鐘分卷切割」與「首尾影格繼承」技術，徹底解決 AI 影片長度限制與畫面閃爍問題，實現真正的企業級全自動化量產。

### 🛡️ 產線防禦與基礎設施鐵律 (The Ironclad Rules)
* **物理路徑鎖死**：保護 Mac M4 的 256GB SSD，所有大容量 I/O (影片下載、截圖、資料庫) 強制綁定 `/Volumes/AI_Workspace/`。 (4/18/2026 開始生效)
* **標準化退出碼與日誌瘦身 (Log Sanitization)**：所有 Python 腳本在捕捉到致命錯誤時，必須強制呼叫 `sys.exit(1)`。寫入 `project_learning.md` 的錯誤日誌必須經過正則清洗，嚴禁寫入動輒數十行的 Stack Trace，僅保留「Exception Name」與「最後一行關鍵錯誤訊息」，防止檔案無限膨脹導致復盤 Token 暴增。
* **指令注入防禦 (Command Injection Prevention)**：在 `auto_editor.py` 或任何呼叫 FFmpeg 等底層命令的腳本中，嚴禁使用 `shell=True`。所有 Subprocess 必須以陣列形式傳遞參數 `["ffmpeg", "-i", ...]`，並對 LLM 生成的檔名/路徑強制執行安全字元過濾，防止惡意 Prompt 注入導致 `rm -rf /` 災難。
* **原生外部機密隔離 (External Secrets)**：徹底淘汰實體 `.env` 檔案。全面導入 OpenClaw 的「外部機密管理」，透過終端機執行 `openclaw secrets configure` 寫入 API 金鑰。腳本運行時透過 `target-path` 驗證動態調用，實現「零明文落地」的軍規級防護。
* **API 級別結構化輸出 (Structured Outputs)**：捨棄易脆斷的正則表達式清洗。所有呼叫大模型的腳本，必須在 API 請求層面強制啟用 `response_format={ "type": "json_object" }` 或給定嚴格 JSON Schema。從底層強制約束 100% 合法 JSON，將防呆成本降至 $0。
* **原子寫入防護 (Atomic Writes)**：針對憑證檔 (`state.json`) 與狀態池，嚴禁直接 `open(file, 'w')`。必須強制寫入 `.tmp` 暫存檔，寫入成功後再呼叫作業系統層級的 `os.replace()` 覆蓋原檔。確保在斷電或磁碟瞬斷時，JSON 檔案 100% 不會發生 0KB 靜默損毀。
* **分卷與首尾影格繼承 (Chunking & Frame Inheritance)**：將 5~10 分鐘的劇本切割為多個 60 秒的卷宗 (Volumes)。在呼叫 Veo API 時，下一卷的第一個分鏡，必須強制讀取上一卷最後一個分鏡的「最後一幀 (Last Frame)」作為 Image Prompt 參考起點，達成 100% 無縫連貫。
* **API 限流與退避防護 (Rate Limit & Exponential Backoff)**：嚴禁在呼叫 Google Cloud API 遇到 `429 Too Many Requests` 時直接崩潰。所有 API 請求必須包裝在指數退避 (Exponential Backoff) 迴圈中，確保產線能在配額受限時優雅休眠與重試。
* **內容合規護欄 (Safety Guardrails)**：Google Veo 具備嚴格的安全審查。LLM 在生成分鏡 Prompt 時，必須注入系統級過濾指令，嚴禁出現暴力、自殘或血腥字眼，一律以「荒謬、誇張的定格動畫肢體」來隱喻職場壓抑，確保 API 通過率 100%。


## ▌ 1. 基礎設施與開源技術棧 (環境部署清單)

開源系統總覽：

🛠️ 核心開源技術棧大解密
1. 底層多智能體框架 (Agent Framework)
* **OpenClaw：**開源的 Meta-Agent 框架。負責扮演總管大腦，讀取 project.yml 並負責排程、派發任務給各個 Python 腳本。
2. 企業級 API 與雲端基礎設施 (Enterprise API)
* **Google GenAI SDK (Vertex AI)：** 產線的核動力引擎。用於穩定介接 Google Veo 影片生成大模型，並支援圖片參考 (Reference Image) 功能以鎖定角色特徵。
3. 極限影音處理引擎 (Media Processing)
* **FFmpeg (ffmpeg)：**全球最強大的開源影音處理底層核心。除了呼叫 Mac M4 硬體加速剪輯外，新增 `concat demuxer` (無損拼接) 職責，負責在產線最後一步，將所有 60 秒的卷宗 (Volumes) 瞬間縫合成 5~10 分鐘的完整大片。
* **yt-dlp (yt-dlp)：**開源的超強影片下載器。負責在研發部（齒輪二）從 YouTube 和 B 站拔取素材。
4. 音效與視覺運算 (Audio/Visual Hack)
* **Librosa (librosa)：**開源的 Python 音訊分析庫。用於「音樂踩點動態剪輯」，精準抓出 MiniMax 配樂的重拍 (Beats)，讓 FFmpeg 根據重拍切換畫面。
* **Pillow (Pillow)：**Python 開源影像處理庫。用於執行我們的「視覺駭客戰術」，將 12 張截圖無縫拼成一張十二宮格大圖，幫你省下海量 API Token。
5. 通訊與輕量資料庫 (Comm & DB)
* **pyTelegramBotAPI (pyTelegramBotAPI)：**開源的 Telegram Bot 封裝庫。用來建立老闆的「人類攔截審批」按鈕介面。
* **SQLite (Python 內建)：**輕量級關聯式資料庫。啟用 WAL 模式後，完美勝任我們的「風格基因金庫」與「財務記帳本」。

【工程團隊注意】：請在 Mac M4 上執行以下指令，建立標準化開發與執行環境。我們不購買外部技能，全數使用穩定開源庫。

1. 系統級依賴 (透過 Homebrew)
請在終端機執行：
brew install ffmpeg

### 2. Python 環境依賴與隔離沙盒 (Virtual Environment)
**CTO 鐵律：絕對禁止污染作業系統全域環境！所有相依套件必須安裝於 `venv` 虛擬沙盒中。**

請在專案根目錄 (`AI_DRAMA_FACTORY`) 下，開啟終端機並依序執行以下指令：

**【Windows 開發測試機環境配置】**
1. 建立虛擬沙盒：`python -m venv venv`
2. 啟動沙盒：`.\venv\Scripts\activate` (成功後提示字元最前面會出現 `(venv)`)
3. 安裝核心套件：`pip install -r requirements.txt`
4. 安裝無頭瀏覽器核心：`playwright install chromium`

**【Mac M4 最終部署機環境配置】**
1. 建立虛擬沙盒：`python3 -m venv venv`
2. 啟動沙盒：`source venv/bin/activate`
3. 安裝核心套件：`pip install -r requirements.txt`
4. 安裝無頭瀏覽器核心：`playwright install chromium`

> ⚠️ **注意**：包含 `.env` 與 `venv/` 等機密或龐大編譯檔案，已預設受到 OpenClaw 系統層保護，不會被 Agent 誤讀或上傳。

（下方保留原有的 requirements.txt 套件清單...）
請將以下內容存為 requirements.txt 

 requirements.txt：
google-genai
librosa==0.10.1
yt-dlp==2024.03.10
Pillow==10.2.0
opencv-python==4.9.0
edge-tts==6.1.9
pyTelegramBotAPI==4.15.4
requests==2.31.0

套件用途對照表：
* google-genai: Google Vertex AI 官方 SDK，介接 Veo 模型。
* playwright: 游擊隊 RPA 與 DOM 哨兵核心。
* beautifulsoup4: 網頁 DOM 節點本地壓縮與去噪。
* librosa: 音樂重拍 (Beats) 分析，用於踩點剪輯。
* yt-dlp: 礦車素材下載器。
* Pillow: 十二宮格影像拼接。
* opencv-python: 透過 SSIM 演算法進行影格去重 ($0 成本過濾)。
* edge-tts: 網路全斷時的終極地端高擬真配音兜底。
* pyTelegramBotAPI: (即將被 OpenClaw 原生協議取代，僅作備用發布)。


1. OpenClaw 底層權限與大腦同步 (project.yml & .clinerules)
* 沙盒權限解放：在 project.yml 的 role_exceptions 中，必須移除 $$VD (視覺導演) 的 playwright 權限，並重新開放 HTTP 請求權限與 Google Vertex AI 的網域白名單，允許其直連外部 API 算圖。
* 工具名單更新：將舊版依賴的 webhook_poller 等廢棄技能從 required_skills 清單中剔除，確保 Meta-Agent 不會因為找不到舊工具而陷入死循環。
* **運行時安全隔離 (OpenShell SSH Sandbox)**：全面擁抱 OpenClaw v2026.3.22 最新架構。將 RPA 引擎與 FFmpeg 等高風險/高負載腳本，全數投入輕量化 SSH 沙盒中執行。這能實現超越「路徑鎖死」的實體級物理隔離，杜絕任何 Agent 越權存取 Mac 主系統。
* **網關看門狗 (Gateway Defibrillator)**：無需安裝外部插件。利用 macOS 原生 `launchd`，為 OpenClaw 主程序與 LiteLLM 聚合網關編寫帶有 `KeepAlive=true` 的守護配置。一旦核心程序因網路波動崩潰，作業系統將在 3 秒內自動將其「電擊救活」，確保產線大腦全年無休。

1. 記憶體與檢索擴充預備 (Future Proofing & Zero-Cost Retrieval)
* **本地檢索神器 (QMD)**：針對未來膨脹的「風格基因庫」與「報錯日誌」，系統深度整合開源本地檢索工具 QMD (The Token Killer)。在 CTO 週末進行產線除錯與復盤時，直接開啟本地 OpenClaw Session 調用 QMD，透過本地 BM25 + 向量運算，精準從 `project_learning.md` 與 `dom_error_snapshot.txt` 抓取 Top-3 關鍵段落給模型分析，達成 $0 API 成本的高效復盤。

---
## ▌ 5. 專案完整檔案結構 (Windows 與 Mac 通用)

AI_DRAMA_FACTORY/                  # 專案總根目錄
│
├── .env                           # ⚙️ 全域環境變數 (TARGET_ENV, API Keys 等)
├── .clinerules                    # 🧠 OpenClaw 最高行為準則
├── project_learning.md            # 📝 啞巴版復盤手冊 (僅留 Exception Name)
├── requirements.txt               # 📦 Python 相依套件清單
├── video_input.txt                # 📋 齒輪二礦車輸入題庫
├── TODO_v7.8_Timeline.md          # 🗒️ 舊版時程備忘 (僅供參考，以 v7.10 為準)
│
├── .auth/                         # 🔐 Session 憑證存放區
│   └── audio_state.json           # [動態生成] Suno/音樂 RPA 登入憑證 (保留音樂 RPA)
│
├── .openclaw/                     # 🧠 OpenClaw 專屬大腦配置區
│   ├── project.yml                
│   ├── OpenClaw_prj_guide_v2.md   
│   ├── agents_registry/           
│   └── workflows/
│       └── epic_longform_workflow.md # [🆕 更新] 5-10 mins 大片產線 SOP
│
├── .chrome_profile_audio/         # 🌐 音樂平台 Chrome CDP 用戶檔案
│
├── scripts/                       # 🛠️ Python 自研腳本
│   │
│   ├── common/                    # 🤝 跨齒輪共用工具 (底層護法)
│   │   ├── env_manager.py         
│   │   ├── atomic_io.py           
│   │   ├── path_sanitize.py       
│   │   ├── llm_client.py          
│   │   └── json_parser_utils.py   
│   │
│   ├── gear2_rnd/                 # ⚙️ 齒輪二：R&D 逆向研發線 (維持不變)
│   │   ├── daily_keyword_mutator.py
│   │   ├── batch_style_analyzer_sqlite.py 
│   │   ├── style_database.py      # (需配置 timeout=30 & WAL)
│   │   ├── vault_manager.py       
│   │   └── quick_manager.py       # 快速任務管理工具
│   │
│   └── gear1_prod/                # 🏭 齒輪一：日常自動化產線 (v8.0 API 版)
│       ├── pipeline_runner.py             # 總指揮官（支援多卷迴圈處理與 Concat）
│       ├── setup_audio_session.py         # Suno 音樂 CDP 劫持
│       ├── audio_rpa_engine.py            # 網頁音樂爬蟲 (保留，因 Suno 尚無平價 API)
│       ├── music_first_orchestrator.py    # [🔄 重構] 分卷中樞 (將 10 mins 音樂切為多份 JSON)
│       ├── veo_api_engine.py              # [🆕 新增] Google Veo API 核心算圖引擎
│       ├── auto_editor.py                 # [🔄 重構] 踩點剪輯台 (支援卷宗無損拼接)
│       ├── title_post_generator.py        # 貼文文案 JSON 產生器
│       ├── image_anchor_generator.py      # 角色圖錨點產生器
│       ├── ledger_manager.py              # 財務記帳本
│       └── cloud_archiver.py              # 打包冷儲存
│
└── assets/                        # 📁 動態資料與資料庫
    ├── schemas/                   # [🆕 待建] 存放 JSON Schema (storyboard/post/receipt)
    ├── scripts/                   # 存放 storyboard.json
    ├── final_exports/             # 存放 mv_60s.mp4, receipt.json, title_post.json
    ├── dom_error_snapshot.txt     # [動態生成] 即夢哨兵崩潰時的 HTML 快照
    ├── dom_audio_snapshot.txt     # [動態生成] 音樂 RPA 崩潰時的 HTML 快照
    ├── budget_ledger.db           # [動態生成] 財務帳本
    ├── data/                      # 存放 style_vault.db 金庫、pain_points.txt 等種子檔
    ├── audio/                     # 存放生成的音樂檔（music_full.mp3 / music_60s.mp3）
    ├── sfx/                       # 存放預設物理白噪音
    ├── image_anchors/             # 存放基準角色圖
    ├── video_clips/               # 存放 RPA 下載的短片（shot_*.mp4）
    └── backup_archive/            # 存放 ZIP 備份檔


## ▌A. v8.0 交付物定義（驗收標準）
每次成功跑完一個 Epic Job（1 支 5~10 分鐘成片），輸出以下檔案到 `assets/`：
1. `assets/audio/<job_id>/music_epic.mp3`（5~10 分鐘完整總譜）
2. `assets/scripts/<job_id>/vol_1.json` 到 `vol_n.json`（分卷腳本，每卷 60s）
3. `assets/video_clips/<job_id>/shots/`（Veo API 回傳的精緻 `.mp4`）
4. `assets/final_exports/<job_id>/epic_movie.mp4`（無損拼接後的最終 5~10 分鐘大片）
5. `assets/final_exports/<job_id>/receipt.json`（剪輯與 API 花費收據）

---

## ▌C. v8.0 核動力產線工作流 (Chunking & API Strategy)

### Phase 1 — 音樂 RPA 生成 (The Epic Master Track)
**腳本**：`scripts/gear1_prod/audio_rpa_engine.py`  
**目標**：生成 5~10 分鐘的完整配樂（Master Track）。此為整部電影的時間軸骨架。

### Phase 2 — 大腦分卷切割 (Chunking Orchestrator)
**腳本**：`scripts/gear1_prod/music_first_orchestrator.py`  
**戰術**：
- 讀取 10 分鐘的 `music_epic.mp3`。
- 使用 `librosa` 抓取節拍，但**強制每 60 秒截斷一次**。
- 將 10 分鐘的資料，輸出成 `vol_1.json`, `vol_2.json` ... `vol_10.json`，每份 JSON 嚴格控制在 12~15 鏡。

### Phase 3 — Veo API 算圖引擎 (First-Frame Inheritance)
**腳本**：`scripts/gear1_prod/veo_api_engine.py` (取代舊有 RPA)  
**戰術**：
- 讀取 `vol_n.json` 發送 API 請求。
- **核心鐵律**：處理 `vol_2.json` 的第一個分鏡時，腳本必須自動讀取 `vol_1.json` 最後一個分鏡的 `.mp4`，擷取最後一幀 (Last Frame)，當作 Image Prompt 餵給 Veo API。這能確保場景與角色的 100% 連貫。
- 內建 Exponential Backoff 抵抗 429 錯誤。

### Phase 4 — 卷宗獨立剪輯與終極縫合 (M4 Concat Assembly)
**腳本**：`scripts/gear1_prod/auto_editor.py`  
**戰術**：
- **Step 1 (獨立剪輯)**：對 `vol_1` 到 `vol_10` 分別執行 M4 `h264_videotoolbox` 踩點剪輯，產出 `clip_vol_1.mp4` 到 `clip_vol_10.mp4`。
- **Step 2 (終極縫合)**：建立 `concat_list.txt`，呼叫 `ffmpeg -f concat -safe 0 -i concat_list.txt -i music_epic.mp3 -c:v copy -c:a aac epic_movie.mp4` 瞬間完成 10 分鐘大片組裝。

---

### Phase 5 — 標題/Hashtags/貼文生成（GLM Structured JSON）
**腳本**：`scripts/gear1_prod/title_post_generator.py`（**新增**）  
**目的**：用最低 token 成本生成可直接貼上平台的文案 JSON。

**輸入（必須短）**：
- `mv_mood`、`visual_constraints`、`music_tags`
- `storyboard.json` 的「shots 摘要」（每段 1 行，不要輸出長篇敘事）

**模型策略（建議）**：
- 主用：智譜 GLM（偏向 GLM-4-Flash 類型，低成本）
- 備援：LiteLLM 聚合網關（舊版已有 Gateway Defibrillator）

**Structured Outputs（必做）**：
- 強制 JSON Schema（或 `json_object`）輸出：
  - `short_titles`（>= 5）
  - `hashtags`（>= 10）
  - `community_posts`（2）
  - `pinned_comment`（1）
  - `compliance_notes`（可選）

**輸出**：
- `assets/final_exports/<job_id>/title_post.json`（原子寫入）

---

### Phase 6 — 財務記帳 + 打包冷儲存 + 垃圾回收
**腳本**：
- `scripts/gear1_prod/ledger_manager.py`
- `scripts/gear1_prod/cloud_archiver.py`

**MVP 財務邏輯（必做調整）**：
- v8.0 財務邏輯（必做調整）：

	- Veo API 按秒計費：必須在腳本中精準核算 Vertex AI 影片生成的呼叫成本（根據 Google Cloud 定價）並寫入帳本。
	- 核算 GLM Token 成本（storyboard + title_post）。
	- 核算音樂端成本與其他雲端存儲流量。

**輸出**：
- `assets/budget_ledger.db`（WAL）
- `assets/backup_archive/<job_id>.zip`（或 S3 key）

---

## ▌D. MVP 必做工程改動清單（與 v7.9 映射）
> 目標：全面汰換 RPA 引擎，把舊版產線升級為能穩定交付 5~10 分鐘連貫大片 (Epic Long-Form) 的核動力產線

### D1. 封印（前 6 個月不用，但保留）
- `edge-tts`（對白/配音全部關閉）
- 字幕相關（SRT/ASS）全部關閉
- `auto_editor.py` 字幕軌處理：加開關 `ENABLE_CAPTIONS=False`，預設不走字幕分支

### D2. 強化（必須優先）
在 `scripts/common/` 新增三個「鐵律落地」工具（MVP P0）：
1. `atomic_io.py`：原子寫入 JSON/文字
2. `path_sanitize.py`：檔名/路徑安全過濾（Command Injection 防線的一部分）
3. `llm_client.py`：統一 GLM + LiteLLM 客戶端，強制 Structured Outputs，統一 retry/timeout/log 瘦身

> 注意：舊版白皮書寫「淘汰 .env」，但 `env_manager.py` 又依賴 `.env`。  
> **MVP 折衷（建議寫進規範）**：  
> - `.env` 僅存非機密（TARGET_ENV、WORKSPACE_ROOT、Topic ID 等）  
> - 所有 API Key/Token/cookie 走 `openclaw secrets configure`（External Secrets 鐵律不變）

### D3. 新增（MVP 必需）
- `scripts/gear1_prod/title_post_generator.py`：成片後的文案輸出器（Structured JSON）
- `assets/schemas/`：放 JSON Schema（storyboard/title_post/receipt）

---

## ▌E. 安全與穩定性鐵律（MVP 仍需 100% 遵守）
### E1. subprocess 禁止 shell=True（硬性）
- FFmpeg / yt-dlp / 任何命令一律 `["ffmpeg", "-i", ...]`
- 對「LLM 生成的檔名/標題」一律先 sanitize 再落地

### E2. Atomic Writes（硬性）
- `storyboard.json`
- `receipt.json`
- `title_post.json`

全部必須 `.tmp` → `os.replace()`。

### E3. Log Sanitization（硬性）
- `project_learning.md`：只記
  - Exception Name
  - 最後一行關鍵錯誤訊息
  - job_id + phase
- 禁止貼長 stack trace（避免復盤 token 暴增）

### E4. Zombie Hunter（硬性）
- 當收到 `sessions_yield` 時，強制呼叫 `/compact` + `hard_clear` 釋放 LLM 記憶體。
- 系統級清剿：任務交接前執行 `pkill -9 -f ffmpeg` 防止剪輯台卡死。

---
---

## ▌F. 下一步（工程落地順序：一週內可見成效）
**Day 1-2（P0）**
- 完成 `scripts/common/atomic_io.py` + `path_sanitize.py` + `llm_client.py`
- `auto_editor.py` 加上 `ENABLE_CAPTIONS=False` 預設

**Day 3-4（P1）**
- `music_first_orchestrator.py` 實作分卷邏輯：讀取 5~10 分鐘音頻，自動循環切割出多份 60s / 12~15 shots 的 `vol_n.json` 腳本。
- `title_post_generator.py` 產出 structured `title_post.json`

**Day 5-7（P2）**
- 「首要任務：開發 veo_api_engine.py 並實作首影格繼承邏輯。」


---

> 本 SOP 文件即為 v8.0 核動力產線的「作戰指令」。  
> 只要嚴格落實：分卷切割、首尾影格繼承、Structured JSON、指數退避限流防護，我們就能穩定量產 5-10 分鐘的企業級高質感微電影。
