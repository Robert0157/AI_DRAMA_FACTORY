# 分發與市場驗證：缺口分析與第一階段補齊 v1

> 對象：CEO｜日期：2026-09-14｜範圍：全庫盤點（F: 控制面＋Y: Mac 執行面）
> 目標對照：**「內容快速分發與市場驗證」**

---

## 一、現況盤點（2026-09-14 實測事實）

### 已有（真實運行中）
| 系統 | 位置 | 狀態 |
|---|---|---|
| 24/7 RTMP 直播堆疊 | Mac（`Streaming/`＋launchd：start_live、reload、concat watcher、unified signal watcher…） | 運轉中 |
| **Shorts 上傳自動化** | Mac `scripts/marketing/`：youtubeuploader＋OAuth（light_music／lofi 各一組）＋14:00/22:00 雙檔期＋meta pool＋LLM 文案＋審計＋Telegram | **運轉中；累計 220 支成功**（light_music 152／lofi 68） |
| 長片上傳 | `long_video_uploader.sh`＋long-upload-v1 sidecar | 機制在；**自 2026-05-12 後無投遞** |
| A 線內容產線 | F: MV pipeline／素材池（166 檔）／Cinemagraph 提示詞 | 就緒、尚未量產影片 |
| B 線劇本鑄造 | script_forge（L1–L4＋閘門） | 鑄造中；**頻道未建立** |

### 斷點（實測證據）
- **Shorts 佇列已空**：`Shorts_Queue/light_music|lofi` 內無任何待上傳 mp4（僅 `_uploaded/` 與歷史檔）；最後成功上傳 **2026-09-09**（已停滞 5 天）。
- 自動化 agent 全部在位（`shorts`、`shorts.lofi`、preupload notice、healthcheck 皆 exit 0）→ **上傳鏈沒有壞，斷的是「內容供給」**。
- 長片：`Long_Queue` 空；直播仍在推。
- **F: 控制面完全看不到以上狀態**（無帳冊、無數據），兩套上傳歷史 schema 互不相通。
- **市場數據回收＝0**：220 支上傳的觀看／按讚／留言從未回收；無 KPI、無決策規則、無回饋到選題。

### 次要異常
- `com.aidramafactory.shorts.launchd_audit` exit 78（稽核 agent 異常）
- `com.aidramafactory.shorts.llm.prewarm` exit 1（文案預熱異常；不影響不跑上傳）

---

## 二、缺口清單（依嚴重度排序）

| # | 缺口 | 嚴重度 | 一階補齊 |
|---|---|---|---|
| 1 | **市場驗證為零**（無數據→無判準→無法淘汰/加碼） | 致命 | ✅ 本次交付 |
| 2 | **內容供給斷鏈**（佇列空、A 線未量產、B 線未上線；渲染 20 步≈2 天/集 vs 檔期 4 集/週＝數學上不可行） | 致命 | ⚠ 需 CEO 決策（見四） |
| 3 | 新格式（175s 影片原生 Shorts）未接軌上傳鏈（舊鏈只認音訊 loop 生成） | 高 | ✅ 節目包契約＋Mac 上傳器 |
| 4 | 分發不可視（控制面看不到上傳實況；schema 分裂） | 高 | ✅ 統一帳冊 |
| 5 | 長片線停擺（5/12 起） | 中 | 決策：沿用或退役 |
| 6 | 合規閘門未掛新格式（水印 QC、合成聲明） | 中 | ✅ 節目包已寫入合成/兒童聲明；水印 QC 上線前掛載 |
| 7 | 稽核/LLM prewarm 異常（exit 78/1） | 低 | 待修（不阻塞） |

---

## 三、本次補齊（第一階段，已交付並驗證）

| 元件 | 落點 | 用途 |
|---|---|---|
| 分發帳冊 | `scripts/marketing/distribution_ledger.py` | 合併兩套上傳歷史 → `assets/data/distribution_ledger.jsonl`；`--check` 節奏報告（實測：light_music／lofi 各 stale 117h／113h、14 天 2/28 支＝under-cadence） |
| 數據回收 | `scripts/marketing/yt_metrics_collector.py` | YouTube Data API v3 公開統計 → `assets/data/market_feedback.db`（快照制）；未設金鑰＝安全跳過 |
| 週報＋評級 | `scripts/marketing/market_validation_report.py` | 節奏＋成長 Top10＋**scale／iterate／retire 評級** → `CEO/05_市場驗證/<ISO週>/報告.md`＋`metrics.json`（`--notify`＝Telegram） |
| 決策規則 | `configs/market_validation_policy.json` | 初始門檻（Shorts：scale≥800 觀看/週＆讚率≥3%；iterate≥200；retire 其餘） |
| 新格式接軌 | `scripts/marketing/episode_bundle.py`＋Mac `publish_episode_shorts.sh` | 175s MV 打包（`episode-short-v1`；9:16、≤185s、fail-closed）→ `Y:/Shorts_Queue/<頻道>/episodes/` → FIFO 上傳、審計回寫 |
| Mac 排程 | `com.aidramafactory.market.metrics`（每日 03:30）＋`market.report`（週日 09:30＋Telegram） | **已安裝完成**（launchctl 確認） |
| 首份週報 | `CEO/05_市場驗證/2026-W38/報告.md` | 已產出（節奏區塊即刻可用；數據區塊待金鑰） |
| 測試 | `tests/test_market_validation.py`（7 項） | 進 CI（106→113） |

---

## 四、需要 CEO 決策（依優先序）

1. **內容供給（最高）**——分發已就緒但無片可發：
   - A 線：以 `Approved_material/`（166 張）先行製作一批 175s MV 進入上傳鏈；渲染引擎替代案（LTX／PC 節點／租雲）待您拍板。
   - B 線：先建頻道＋OAuth（無頻道則 56 集無處可發）。
2. **YouTube Data API Key**（免費，1 分鐘）：設於 Mac `market_validation.env` → 隔日起觀看/按讚自動回收、週報帶評級。
3. **長片線**：沿用（補投遞）或正式退役（推薦：先退役觀望，資源集中 Shorts）。
4. **舊庫存政策**：Shorts 佇列空 → 是否以既有母帶重建一批 loop 短片上傳以維持頻道活躍？（既有節奏 14/22 已停 5 天；本包未自動重啟以避免未決策下發布舊庫存。）

---

## 五、誠實聲明（限制）

- 續看率／CTR 需 YouTube Analytics API（OAuth `yt-analytics.readonly`）＝**Phase 2**，本階段無法取得。
- 評級門檻為**初始值**：累積一週數據後校準（policy 檔可調，不需改碼）。
- 本包**未重啟任何上傳、未動 Mac 既有腳本**（僅新增檔案）；所有改動 fail-closed。
