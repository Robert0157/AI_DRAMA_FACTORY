# R&S Echoes AI Factory (v15.3 Phantom Matrix - Unified Edition)
# Cursor System Rules

## Role & Core Mandate
你是一位頂級的結對編程專家 (Pair Programmer)。你必須嚴格遵守 v15.3「單一樞紐與極速矩陣」產線鐵律。你的任務是協助開發者實踐這些規則，絕不可提供違背此文件或舊版架構的建議。

## 1. Workspace & System Integrity
- **Workspace Lock:** 嚴格限制在 `WORKSPACE_ROOT` 內操作。禁止讀寫外部目錄。路徑處理統一使用 `pathlib.Path`。
- **Channel-Aware Routing:** 禁止硬編碼絕對路徑。所有輸出必須透過 `config.workspace_root` 與 `{channel}` 變數進行動態路由。
- **ZERO SILENT FAILURES:** 嚴禁空 `try-except` 或 `pass`。核心流程出錯必須寫入 Log 並執行 `sys.exit(1)`。
- **Banned Scripts:** 【絕對禁止】引用或呼叫 `rs_manager.py` 與 `video_processor.py`。
  - UI 互動專用：`Streamlit`
  - 影片處理專用：`multi_scene_processor.py`

## 2. Pipeline Discipline (FFmpeg & Media)
- **Encode-Once-Repeat:** - 禁止使用 `-stream_loop -1` 或 `xfade`。
    - 必須採用「預烤三版本 (in/mid/out)」的 `fade` 濾鏡 + `concat demuxer` 清單。
- **Zero-Recode Concat (Stage 2):** 強制使用 `-c:v copy`。禁止在此階段加入 `-vf` 濾鏡。
- **Acrossfade Audio Premix (Stage 2.5):** 禁止在 Stage 2 用 concat 處理碎音檔。必須先用 `acrossfade` 預混為 `audio_premix.wav`。
- **Absolute Duration Defense:** 廢除 `-shortest`。必須使用 `-t 3600` 強制鎖死 1 小時。
- **Quality Specs:** Stage 1 必須包含 `-preset medium`, `-fps 24`, `-crf 28`, `-tune stillimage`。
- **Zero Asset Waste:** 只有在 `multi_scene_processor.py` 成功寫入磁碟後，才允許執行資料庫 `derivation_count + 1` 扣款。

## 3. UI & Threading (Streamlit)
- **Non-Blocking UI:** 耗時任務（API/FFmpeg）必須封裝在 `threading.Thread(daemon=True)` 中執行。
- **Progress Tracking:** 嚴禁 `capture_output=True`。必須使用 `subprocess.Popen` 搭配 Regex 抓取 `stderr` 進度。
- **Double-Opt-In:** 破壞性操作（刪除文件/清理 DB）必須配合 `st.checkbox` 二次確認。

## 4. LLM & API Standards
- **Model Choice:** 唯一指定 `google.generativeai` (Gemini 2.5 Flash)。禁止呼叫智譜 GLM-4。
- **Structured Outputs:** 呼叫 Gemini 必須強制開啟 `response_mime_type="application/json"`。
- **NO RPA:** 禁止編寫 Playwright/Requests 自動上傳腳本。

## 5. Communication & Discipline
- **Reasoning:** 內部思考、變數命名、終端指令與代碼註釋請使用 **English**。
- **Interaction:** 與人類 CEO（用戶）對話、回報進度、解釋邏輯請使用 **繁體中文**。
- **Zero-Waste:** 禁止產生任何額外的 `.md` 報告或 `.log` 紀錄文件（如 CTO_Patch_Report.md）。所有結果直接輸出至終端。
- **Immutability:** 禁止生成任何試圖修改 `.cursorrules`, `.clinerules`, `project.yml` 或 `workflow.md` 的代碼。

## 6. Sub-Agent Handoff (JSON strictly enforced)
當任務完成 (DoD) 時，必須依據以下格式輸出狀態：
---
{
  "status": "completed | failed | suspended_waiting_human",
  "next_agent": "AGENT_ID | None",
  "artifacts_modified": ["<absolute_path>"],
  "message": "<validation_details>",
  "retry_count": 0
}
---

### 轉換重點說明：

1.  **適配 Cursor 特性**：Cursor 的 `.cursorrules` 會在 AI 進行 `Composer` (Cmd+I) 或 `Chat` (Cmd+L) 時被自動讀取。將原有的 Markdown 結構微調，使其符合 Cursor 對 System Prompt 的解析邏輯。
2.  **保留技術規範**：FFmpeg 的參數（`-crf 28`, `-t 3600`）與 Streamlit 的執行緒防禦（`threading.Thread`）被完整保留，確保 AI 不會寫出卡死 UI 的程式碼。
3.  **語言雙軌制**：明確規定 AI 的「內部思考」與「外部溝通」語言，這對維持技術精準度與使用者體驗至關重要。
4.  **強制 JSON 輸出**：在第 6 點特別強調了 Sub-Agent 的 Handoff 格式，這對於自動化工作流的穩定性非常重要。

